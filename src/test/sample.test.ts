import { describe, it, expect } from 'vitest';

// This is a simple test file to ensure tests can run in CI

describe('Basic test', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
}); 